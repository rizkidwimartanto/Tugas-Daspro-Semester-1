#include <stdio.h>
#include <stdlib.h>

int main()
{
    int perulangan = 1;
    do{
        printf("Perulangan Ke %d\n", perulangan);
        perulangan++;
    int angka = 1;
    while(angka <= 5){
        printf("Angka Ke %d\n", angka);
        angka++;
    }
    }
    while(perulangan <= 7);
    return 0;
}
