#include <stdio.h>
#include <stdlib.h>

int main()
{
    int perulangan;
    for(perulangan = 0; perulangan <= 7; perulangan++){
        printf("Perulangan Ke %d\n", perulangan);
    int angka = 1;
    do{
        printf("Angka Ke %d\n", angka);
        angka++;
    }
    while(angka <= 5);
    }
    return 0;
}
