#include <stdio.h>
#include <stdlib.h>

int main()
{
    int perulangan = 1;
    while(perulangan <= 7){
        printf("Perulangan Ke %d\n", perulangan);
        perulangan++;
    int angka;
    for(angka = 1; angka <= 5; angka ++){
        printf("Angka Ke %d\n", angka);
    }
    }
    return 0;
}
